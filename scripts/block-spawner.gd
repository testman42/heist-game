extends Node
class_name BlockSpawner

# Spawns level blocks as the player is moving forward.
# All blocks *must* be 100x100 meters.

# path to the first block which must already be in the level
export(NodePath) var firstBlock



